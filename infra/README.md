# Infraestructura multicloud

Infraestructura como codigo para Azure, AWS y GCP usando modulos con interfaces comunes.

## Estructura esperada

- `modules/`: networking, compute, database, storage, secrets.
- `environments/dev|staging|prod`: composicion por entorno.

La implementacion inicial se completa en fases posteriores del plan.
